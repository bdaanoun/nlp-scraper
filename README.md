```
Define keywords → "pollution", "deforestation", "toxic waste"
        ↓
Convert them to numbers (embeddings)
        ↓
Convert article sentences to numbers (embeddings)
        ↓
Measure distance between keyword numbers and sentence numbers
        ↓
Close distance = possible scandal
```